# Open a simulation skeleton

Creates and opens a .R file containing a skeleton for writing a Monte
Carlo simulation study.

## Usage

``` r
create_skeleton()
```

## Examples

``` r
if (FALSE) { # \dontrun{
create_skeleton()
} # }
```
